字节流和字符流
字节流：相对于而言，属于原生的操作(未经过处理  直接使用)
	输入：inputStream
	输出：outputStram
字符流：相对于而言，再输入和输出操作中，对其进行处理操作(再字节流的操作中添加了处理操作)
	输入：reader
	输出：writer
	
输入输出操作是相对于  内存   而言(面向对象编程)   输入就是读到内存中    输出就是从内存中将其取出来